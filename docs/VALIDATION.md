# Проверка MH Sidebar v0.5.1 — 23 сентября 2026

Автозапуск перенесён из реестра текущего пользователя в интерактивную задачу Планировщика Windows с уровнем `Highest`. При первом сохранении настроек он включён по умолчанию; отказ от автозапуска не требует UAC. Для уже включённого старого автозапуска предусмотрена миграция при запуске новой версии. Включение, выключение и миграция удаляют старую запись `Run` только после успешной настройки задачи.

- `cargo test --locked --offline` — **75 успешно**, 5 аппаратных тестов исключены из обычного запуска. Новые проверки покрывают первый запуск, отказ от автозапуска, формирование интерактивной задачи с повышенными правами, экранирование пути EXE, удаление старой записи и разбор обоих сценариев Windows PowerShell.
- `cargo fmt --check`, `cargo clippy --locked --offline --all-targets -- -D warnings` и release-сборка прошли без предупреждений.
- Полный `tools/gui-smoke.ps1` не смог открыть тестовый экземпляр: пользовательский MH Sidebar уже держит общий single-instance mutex. Работающий экземпляр не завершался. Текущая изолированная сессия не имеет прав на создание задачи Планировщика; подтверждение UAC, реальное создание задачи и запуск после входа в Windows требуют отдельной проверки в обычной учётной записи администратора.
- Пакет `dist/v0.5.1` и архив `dist/MH-Sidebar-v0.5.1-windows-x64.zip` собраны с проверкой версии, PE x64, состава и SHA-256 файлов.

---

# Проверка MH Sidebar v0.5.0 — 23 сентября 2026

Этап «Совместимость и выпуск»: матрица доступных и непроверенных конфигураций, проверка двух мониторов с разным DPI, воспроизводимый замер ресурсов и изолированный portable-пакет с проверкой каждого файла. Подробная матрица находится в `docs/COMPATIBILITY.md` и включается в пакет как `COMPATIBILITY.md`.

- `cargo test --locked --offline` — **68 успешно**, 5 аппаратных тестов исключены из обычного запуска; `cargo test --locked --offline --lib -- --ignored --nocapture` — **5 успешно** на текущем ПК. `cargo fmt --check`, `cargo clippy --locked --offline --all-targets -- -D warnings` и release-сборка прошли без предупреждений.
- Windows 11 25H2, сборка 26200.9457: два монитора с масштабом 150% и 125%. `compatibility_probe` подтвердил физические координаты. `dock_probe` на обоих мониторах проверил AppBar справа/слева и восстановление рабочей области при скрытии и завершении собственного окна. Изолированная среда не допускала регистрацию AppBar; живая проверка была выполнена в обычном сеансе. Первоначальный сбой пробы вызван отсутствием DPI awareness у самого тестового EXE; после включения Per-Monitor V2 он не повторился. Исходный код приложения для этого менять не потребовалось.
- 30 замеров release EXE за 30,36 с после двух секунд прогрева: CPU 0,78 с, в среднем 0,11% от 24 логических процессоров; пиковый рабочий набор 168,2 МиБ, приватная память 163,0 МиБ, дескрипторы 460, потоки 19. Использован отдельный конфиг без автозапуска и резервирования места; завершение с кодом 0. JSON и stderr сохранены в `target/compatibility/`.
- `verify-release.ps1` проверен на полном наборе файлов v0.4.0 в тестовом каталоге: корректный набор принят, изменение README без обновления SHA-256 отклонено. Скрипт выпуска берёт версию из Cargo, проверяет точный состав и хеш каждого файла, версию EXE и PE x64, а CI публикует только ZIP текущего выпуска и его контрольную сумму. Встроенный манифест release EXE извлечён Windows `mt.exe`: версия `0.5.0.0`, DPI awareness `PerMonitorV2`.
- ZIP v0.5.0 распакован в отдельный тестовый каталог; повторная проверка полного состава и SHA-256 прошла. Запуск именно EXE из распакованного архива с отдельным конфигом на пять секунд завершился с кодом 0.
- Исходники `src`, `tests`, `examples`, `build.rs` и PowerShell-скрипты проверены на отсутствие комментариев по правилу ZERO COMMENTS. Сценарии Windows 10, физической смены дисплея или DPI, сна/пробуждения и перезапуска Explorer на этой машине не выполнялись; их отсутствие в матрице нельзя считать успешной проверкой.

Каталог выпуска: `dist/v0.5.0`; архив: `dist/MH-Sidebar-v0.5.0-windows-x64.zip`; SHA-256 архива находится в файле `.zip.sha256`, а контрольные суммы файлов внутри — в `SHA256SUMS.txt`. При завершении тестового EXE библиотека трея продолжает выводить `Error removing system tray icon` при коде завершения 0.

---

# Проверка MH Sidebar v0.4.0 — 23 сентября 2026

Этап «Аналитика»: увеличенный график по нажатию на показатель, статистика минимума/среднего/максимума, CSV выбранного показателя и всей сохранённой истории сеанса, настраиваемые пороговые предупреждения Windows.

- `cargo test --locked --offline` — **68 успешно**, 5 аппаратных тестов исключены из обычного запуска. Новые тесты проверяют разрывы истории, границы окна статистики, экранирование CSV, несколько устройств, сохранение исчезнувшего показателя в экспорте, длительность превышения и паузу уведомлений, отсутствие уведомлений по устаревшим данным, миграцию схемы 3 и отдельную копию при повторной миграции изменённого файла.
- `cargo fmt --check`, `cargo clippy --locked --offline --all-targets -- -D warnings`, debug и release сборки — успешно, без предупреждений. Исходники `src`, `tests`, `examples` и `build.rs` проверены на отсутствие комментариев.
- В отдельном debug-запуске с реальными показателями визуально проверен `target/v04-visual-final/analytics.png`: заголовок, источник, график, статистика и обе кнопки CSV видны. Страница предупреждений также просмотрена в `target/v04-alerts-visual/settings.png`. EXE из portable-пакета запущен на 5 секунд с отдельным конфигом в обычном сеансе и завершился с кодом 0. На закрытии библиотека трея вывела в stderr `Error removing system tray icon`; это не аварийное завершение.
- Ревью выявило три риска потери данных: общий CSV пропускал исчезнувший показатель, экспорт мог перезаписать активный файл настроек, повторная миграция могла принять старую резервную копию за копию изменённого файла. Добавлены регрессионные проверки и исправления; повторное ревью подтвердило все три.
- История остаётся только в памяти и ограничена выбранным окном; среднее арифметическое считается по доступным замерам. Нативный диалог сохранения CSV и показ уведомления Windows на реальном пороговом событии вручную не проверены. Реальное отключение устройства и аппаратные сценарии из предыдущих отчётов требуют отдельной проверки.

Схема настроек 4 хранит правила и выключенный по умолчанию переключатель уведомлений. Резервные копии схем 1–3 сохраняют точное содержимое старого JSON. Portable-пакет расположен в `dist/v0.4.0`; EXE версии ресурса 0.4.0, размер 11 525 120 байт, SHA256 `b52bd7d0639fa4d86cf132460d027d7de3a822372372a7f113e02d4538c37cc5`.

---

# Проверка MH Sidebar v0.3.0 — 23 сентября 2026

Этап «Удобство»: встроенные и пользовательские профили, импорт и экспорт JSON через стандартные диалоги Windows, порядок блоков перетаскиванием, плотность панели, запись сочетаний клавиш нажатием.

- `cargo test --locked --offline` — **58 успешно**, 5 аппаратных тестов исключены из обычного запуска. Тесты проверяют сохранение машинных настроек при выборе профиля, миграцию схемы 2 с резервной копией, проверку импорта, порядок блоков, захват и отмену сочетания, временное снятие и восстановление глобальных сочетаний.
- `cargo clippy --locked --offline --all-targets -- -D warnings`, `cargo fmt --all`, debug и release сборки — успешно. Исходный код `src`, `tests`, `examples` и `build.rs` проверен на отсутствие комментариев по правилу ZERO COMMENTS.
- Отдельный GUI-запуск debug-версии с реальными показателями сохранил `target/v03-visual/settings.png` и `sidebar.png`; снимок страницы профилей проверен визуально. Пакетный EXE запущен в обычном сеансе с отдельным конфигом и `--smoke-test 5`; процесс завершился с кодом 0. При остановке вывел в stderr `Error removing system tray icon` — это ограничение проверки значка трея, а не падение приложения.
- Независимое ревью выявило потерю Win-модификатора при записи клавиши. Исправлено чтением состояния клавиш Win через Windows API; ревьюер подтвердил исправление. Интерактивное нажатие в нативном файловом диалоге и перетаскивание мышью пока не проверены.
- Portable-пакет: `dist/v0.3.0/MH-Sidebar.exe`, 11 324 928 байт, SHA256 `607e4d125092d8d761f3c2cd3cebf7b527daf29ca681213ee4cab55c4330ee42`. Пакет содержит README, лицензии и список контрольных сумм; предыдущие пакеты сохранены отдельно.

Схема настроек 3 сохраняет профили и плотность. При загрузке схемы 1/2 сначала создаётся `settings.v1.json` или `settings.v2.json`. Импорт меняет только черновик до применения. Профили не меняют системные параметры панели.

---

# Проверка MH Sidebar v0.2.0 — 23 сентября 2026

Этап «Устройства и сбор данных»: постоянные ID, схема настроек 2 с резервной копией v1, шесть независимых источников, индивидуальное состояние свежести, история отключённых устройств и сохранённые карточки.

- `cargo test --locked --offline` — **50 успешно**, 5 аппаратных тестов исключены из обычного запуска.
- `cargo test --locked --offline --lib -- --ignored` — **5 успешно**. Проверены GPU/WDDM/PDH, уникальность ID томов/сетевых интерфейсов и совпадение ID RTX 5070 Ti между NVML и WDDM.
- `cargo run --locked --offline --example collection_probe` — все шесть независимых источников завершили замеры, 22 устройства на текущем ПК. Проверка не открывает GUI и не меняет пользовательские настройки.
- `cargo fmt --check`, `cargo clippy --locked --offline --all-targets -- -D warnings`, debug и release сборки — успешно.
- Регрессионные проверки: переименование, одинаковые имена, неоднозначные индексы одинаковых GPU, отложенное сопоставление отсутствующих устройств, сохранение исходного JSON в `settings.v1.json`, независимое устаревание CPU/PawnIO, отказ и восстановление источника, незаблокированное завершение при зависшем работнике, отсутствие повторных точек истории, разрыв при отключении и удаление сохранённой карточки по истечении окна.
- Независимое ревью выявило два замечания: невидимую сохранённую историю отключённого устройства и неоднозначную миграцию одинаковых GPU по индексу. Оба исправлены и подтверждены повторным ревью.
- Реальные framebuffer-снимки debug-панели и настроек просмотрены (`target/v02-visual` и `target/v02-visual-narrow`); запуск с отдельным конфигом завершился кодом 0.
- Полный `tools/gui-smoke.ps1` **не прошёл**: в песочнице недоступен курсор; вне неё координатные клики по заголовку нестабильны при текущем масштабировании. В отдельных прогонах проверены Reset/Cancel, повторное открытие и Reset/Apply, но не весь сценарий целиком. Временные попытки менять координаты/маршрут теста не оставлены. Сохранены только исправления кодировки скрипта (UTF-8 BOM для PowerShell 5.1) и записи тестового JSON (UTF-8 без BOM). Проверки поведения приложения не ослаблены.
- Готовый `dist/v0.2.0/MH-Sidebar.exe`: ресурс версии Windows **0.2.0**, размер **11 095 552 байт**, SHA256 **376b66c8e73dfb5f0a21a181c29d58011bcbc5c3fe7fcae90315db2f84b4a948**. Release запущен на 5 секунд с отдельным конфигом в обычном Windows-сеансе: код 0, stderr пуст. В песочнице завершение было с кодом 0, но библиотека трея сообщала `Error removing system tray icon`.

Оставшиеся аппаратные проверки: реальное переподключение диска/сетевого адаптера, изменение буквы тома, несколько физических GPU, перезагрузка с изменением LUID, смена DPI/сон/Explorer. Программные сценарии не заменяют эти проверки. Если Windows не выдаёт постоянный ID или нельзя сопоставить NVML с WDDM, ограничение показано в настройках и описано в README. Зависший системный вызов не прерывается; его поток не блокирует другие источники и выход интерфейса, но сам источник требует перезапуска приложения.

Справка по использованным Windows API: [InterfaceGuid и флаг FilterInterface](https://learn.microsoft.com/en-us/windows/win32/api/netioapi/ns-netioapi-mib_if_row2), [путь GPU](https://learn.microsoft.com/en-us/windows/win32/api/wingdi/ns-wingdi-displayconfig_adapter_name), [PCI-адрес GPU](https://learn.microsoft.com/en-us/windows-hardware/drivers/ddi/d3dkmthk/ns-d3dkmthk-_d3dkmt_adapteraddress).

---

Ниже — исторические проверки предыдущих версий.

# Проверка MH Sidebar v0.1.1 — 22 сентября 2026

Изменения: раздельные температурные пороги CPU/GPU с переносом прежних значений, автоматическое переоткрытие CPU-датчика после трёх пустых замеров, ограниченная экспоненциальная задержка и диагностика восстановления. Версия интерфейса берётся из Cargo.toml.

- `cargo test --locked --offline` — 38 успешно, 4 аппаратных проверки исключены из стандартного запуска.
- `cargo test --locked --offline --lib -- --ignored --nocapture` — все 4 аппаратных проверки успешно (GPU/WDDM/PDH).
- `cargo clippy --locked --offline --all-targets -- -D warnings` — успешно.
- `cargo fmt --check` — успешно.
- `cargo build --locked --offline` и `cargo build --release --locked --offline` — успешно.
- Переносимая сборка: `dist/v0.1.1/MH-Sidebar.exe`, версия ресурса Windows 0.1.1, размер 11 059 200 байт. SHA256: `196389e0092e36de0c76af3acd7b77059b42d672de476830ca9c2fb0adbae41a`. Версия размещена отдельно, чтобы не заменять используемый EXE. Интерактивный запуск этой release-сборки не проверен.
- Тесты восстановления используют реальные переходы контроллера и подставленные операции открытия/чтения: три пустых замера, временный сбой, частичные показания, NaN/Infinity, отказ повторного открытия, задержки 1–30 секунд, восстановление и неподдерживаемый CPU. Переоткрытие без данных не сбрасывает задержку. Новая ошибка открытия заменяет прежнюю причину отсутствия показаний.
- Настройки: перенос пользовательских порогов из v0.1.0, независимое изменение CPU/GPU, нормализация диапазонов, сохранение и повторная загрузка.
- `tools/gui-smoke.ps1`: исправлена кодировка UTF-8 BOM для Windows PowerShell 5.1. После этого скрипт запускается, но проверка завершилась ошибкой `Settings window did not open`. В сеансе уже работает MH Sidebar (PID 19320), приложение использует общий single-instance mutex. Работающий пользовательский экземпляр не останавливался. Интерактивное подтверждение нового экрана порогов остаётся невыполненным.

Ограничения: физический отказ/перезапуск PawnIO с последующим восстановлением на оборудовании не воспроизводился; проверена программная последовательность отказов. Контроллер не прерывает зависший IOCTL. При наличии хотя бы одного показателя CPU частичный отказ не запускает переоткрытие. Горячее подключение мониторов, DPI, сон/пробуждение и перезапуск Explorer требуют отдельной ручной проверки.

---

Ниже — исторический отчёт v0.1 от 19 сентября; его список функций, число тестов и SHA256 не описывают v0.1.1.

# Проверка MH Sidebar v0.1 — 19 сентября 2026

Компактная верстка: уменьшены поля панели, высота строк и разделителей; графики рисуются под текстом без отдельного места по высоте. Проверены реальный framebuffer (`screenshots/sidebar.png`), `cargo fmt --check`, Clippy всех targets без предупреждений и запуск обновлённого release EXE в обычном Windows-сеансе (код 0, stderr пуст). SHA256 обновлённой сборки: `acd1f2b4c7284851b5128f096729878c388ccf17bb335721ce366c83ff3bf1e3`. Результаты ниже относятся к исходной версии до этой визуальной правки.

- `cargo fmt --check` — успешно.
- `cargo test --locked --offline` — 19 успешно, 4 аппаратных проверки исключены из стандартного CI-запуска.
- `cargo test --locked --offline --lib -- --ignored` — все 4 аппаратных проверки успешно.
- `cargo clippy --locked --offline --all-targets -- -D warnings` — успешно, без предупреждений.
- `cargo run --offline --example probe` — реальные показания Ryzen 9 5900X, RTX 5070 Ti, RAM, дисков и сетевых интерфейсов; NVML и отдельный WDDM fallback проверены.
- `cargo run --offline --example dock_probe` вне песочницы — успешно: рабочая область второго экрана `(2560,357)-(4480,1389)` уменьшилась справа до `4200`, после переноса слева начиналась с `2840`, после скрытия и Drop восстановилась точно.
- `tools/gui-smoke.ps1` вне песочницы — успешно: настройки открылись; Reset/Cancel сохранили ширину 360; кнопка панели открыла настройки повторно; Reset/Apply сохранили ширину 280; кнопка скрытия скрыла панель.
- Регрессионный тест горячих клавиш дважды меняет регистрацию и отправляет WM_HOTKEY только собственному окну: удалённые сочетания не срабатывают, новые доставляются через исходный канал команд.
- Получены и визуально просмотрены реальные egui framebuffer-снимки: `screenshots/settings.png`, `screenshots/sidebar.png`. Это не макеты.
- Готовый `dist/MH-Sidebar.exe` версии 0.1.0 запущен в обычном сеансе Windows с отдельным тестовым конфигом: код завершения 0, stderr пуст. Размер 11 001 856 байт; SHA256 совпадает с `dist/SHA256SUMS.txt`: `db4724239dc2a803eb68ab2181503c74ca949238dd384035903f3d1c734a0f0e`.

В песочнице регистрация AppBar отклонялась из-за отсутствия доступа к реальному Explorer. В обычном сеансе проверка прошла. Визуальная проверка из песочницы использует framebuffer самого приложения, без захвата чужих окон.

Ручная матрица физического переподключения мониторов, изменения DPI и перезапуска Explorer не выполнена. Соответствующие обработчики есть, но полной проверки на различных конфигурациях пока нет. CPU temperature/power, RAM module speed и SMART не входят в эту версию. EXE не подписан.
